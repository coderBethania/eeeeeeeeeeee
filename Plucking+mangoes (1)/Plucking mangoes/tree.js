class Tree{
    constructor(x,y){
        var options={
            isStatic:true
        }
        this.Tree = bodies.rectangle(x,y,options)
        this.loadimage = loadimg("Plucking mangoes/tree.png")
    }
}